# External Hints Module

Prototype webtrees module that adds a “Hints” tab to individual pages,
queries external providers, and returns JSON hint payloads that can be
turned into sources/notes. A mock provider is included to show the flow.
